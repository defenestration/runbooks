baseURL: "https://defenestration.github.io/runbooks"
languageCode: "en-us"
title: "kube-prometheus runbooks"

theme: book

params:
  BookRepo: "https://github.com/defenestration/runbooks"
  BookEditPath: "edit/main"
  BookSearch: true
  BookSection: "runbooks"
  #BookMenuBundle: "/menu"

markup:
  goldmark:
    renderer:
      # Allows rendering HTML tags. This would only be truly "unsafe" if we were accepting untrusted user input and rendering it, which we aren't.
      unsafe: true
